# SQLite（現在使用中）
default: &default
  adapter: sqlite3
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  <<: *default
  database: db/development.sqlite3

test:
  <<: *default
  database: db/test.sqlite3

production:
  <<: *default
  database: db/production.sqlite3

# PostgreSQL（Dockerにロールバックする場合は以下をアンコメントし、上のSQLite設定を削除する）
# default: &default
#   adapter: postgresql
#   encoding: unicode
#   host: <%= ENV.fetch("DATABASE_HOST", "postgres") %>
#   port: 5432
#   username: <%= ENV.fetch("DATABASE_USERNAME", "business_user") %>
#   password: <%= ENV.fetch("DATABASE_PASSWORD", "business_password") %>
#   pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
#
# development:
#   <<: *default
#   database: business_api_development
#
# test:
#   <<: *default
#   database: business_api_test
#
# production:
#   <<: *default
#   database: business_api_production
